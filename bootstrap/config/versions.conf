# Pinned versions for all third-party software installed by bootstrap.
# To update a tool: change the version, then fill in the new sha256 values.
# Obtain sha256 with: curl -fsSL <release-url> | sha256sum
# Bootstrap will refuse to run if any sha256 variable below is empty.

# --- Go ---
GO_VERSION="1.23.4"
# sha256 of go1.23.4.linux-{amd64,arm64}.tar.gz from https://go.dev/dl/
GO_SHA256_AMD64="6924efde5de86fe277676e929dc9917d466efa02fb934197bc2eba35d5680971"
GO_SHA256_ARM64="16e5017863a7f6071363782b1b8042eb12c6ca4f4cd71528b2123f0a1275b13e"

# --- fnm (Node version manager) ---
# Release: https://github.com/Schniz/fnm/releases
# FIXME: verify these hashes from the release page before running bootstrap
FNM_VERSION="1.38.1"
# sha256 of fnm-linux.zip (same zip for both arches — fnm detects arch at runtime)
FNM_SHA256_AMD64="FIXME:sha256_of_fnm-linux.zip_for_amd64"
FNM_SHA256_ARM64="FIXME:sha256_of_fnm-linux.zip_for_arm64"

# --- uv (Python package/project manager) ---
# Release: https://github.com/astral-sh/uv/releases
# FIXME: verify these hashes from the release page before running bootstrap
UV_VERSION="0.5.10"
# sha256 of uv-x86_64-unknown-linux-musl.tar.gz / uv-aarch64-unknown-linux-musl.tar.gz
UV_SHA256_AMD64="FIXME:sha256_of_uv-x86_64-unknown-linux-musl.tar.gz"
UV_SHA256_ARM64="FIXME:sha256_of_uv-aarch64-unknown-linux-musl.tar.gz"

# --- Bun ---
# Release: https://github.com/oven-sh/bun/releases
# FIXME: verify these hashes from the release page before running bootstrap
BUN_VERSION="1.1.42"
# sha256 of bun-linux-x64-baseline.zip / bun-linux-aarch64.zip
BUN_SHA256_AMD64="FIXME:sha256_of_bun-linux-x64-baseline.zip"
BUN_SHA256_ARM64="FIXME:sha256_of_bun-linux-aarch64.zip"

# --- Rust (rustup-init) ---
# Release: https://static.rust-lang.org/rustup/archive/<version>/
# FIXME: verify these hashes before running bootstrap
RUSTUP_VERSION="1.27.1"
# sha256 of rustup-init binary from static.rust-lang.org/rustup/archive/
RUSTUP_SHA256_AMD64="FIXME:sha256_of_rustup-init_x86_64-unknown-linux-gnu"
RUSTUP_SHA256_ARM64="FIXME:sha256_of_rustup-init_aarch64-unknown-linux-gnu"

# --- NVIDIA (used by role 35-gpu) ---
NVIDIA_DRIVER_BRANCH="nvidia-driver-550-server"
CUDA_KEYRING_DEB="cuda-keyring_1.1-1_all.deb"

# --- Claude Code container image (used by role 70) ---
CLAUDE_CODE_VERSION="latest"
AGENT_BASE_IMAGE="node:22-slim"
