{ config, lib, pkgs, ... }:
{
  # Phase 2: port bootstrap/roles/43-caddy.sh
  # Covers: bootstrap.CADDY.1-5
  # services.caddy, Caddyfile with /health endpoint, conf.d include pattern, open ports 80+443
}
