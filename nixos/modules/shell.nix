{ config, lib, pkgs, ... }:
{
  # Phase 2: port bootstrap/roles/50-shell.sh
  # Covers: bootstrap.SHELL.1-4
  # programs.zsh, set default shell, machine PATH entries in zshrc.local, minimal root zsh config
}
