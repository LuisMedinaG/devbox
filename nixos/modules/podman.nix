{ config, lib, pkgs, ... }:
{
  # Phase 2: port bootstrap/roles/42-docker.sh
  # Covers: bootstrap.DOCKER.1-7
  # virtualisation.podman (rootless), per-user socket, subuid/subgid, docker-compose shim
}
